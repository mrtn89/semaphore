---
- name: Sudo Test
  hosts: all
  gather_facts: false
  become: true
  become_method: sudo
  become_user: root

  tasks:
    - name: Prüfen welcher User aktiv ist
      command: whoami
      register: whoami_result

    - name: Ausgabe anzeigen
      debug:
        var: whoami_result.stdout
